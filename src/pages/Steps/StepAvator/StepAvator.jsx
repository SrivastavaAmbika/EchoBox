import React from 'react'

const StepAvator = ({onNext}) => {
  return (
      <div>StepAvator
       <button onClick={onNext}>
          Next
          </button>
      
      </div>
  )
}

export default StepAvator